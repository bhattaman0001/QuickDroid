package com.example.moviemagnet.viewmodels

import android.app.*
import androidx.lifecycle.*
import com.example.moviemagnet.repository.*

class MainViewModel(val app: Application, private val repository: Repository) : AndroidViewModel(app) {

}